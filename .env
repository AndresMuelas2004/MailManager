# Docker Environment Configuration
# Place your credential files in the root "credentials" folder (create it if missing)
# and ensure the filenames match below, or update these paths.

MIA_GMAIL_CREDENTIALS_PATH=/app/credentials/gmail_credentials.json
MIA_OUTLOOK_CREDENTIALS_PATH=/app/credentials/outlook_credentials.json
MIA_TOKEN_PATH=/app/backend/data/tokens
