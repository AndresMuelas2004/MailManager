services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mailmanager-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./credentials:/app/credentials
    env_file:
      - .env
    restart: unless-stopped
